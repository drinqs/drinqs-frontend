mutation UpdateReview(
  $cocktailId: ID!
  $liked: Boolean
  $bookmarked: Boolean
) {
  review(
    cocktailId: $cocktailId
    liked: $liked
    bookmarked: $bookmarked
  ) {
    review {
      liked
      bookmarked
    }
  }
}
